"""Configuration package for environment settings and availability.

This package centralizes environment-derived settings and lightweight
capability checks to avoid scattered os.getenv calls across the codebase.
"""